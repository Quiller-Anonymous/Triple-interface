# Step 5 checklist: discharge `ε₁/ε₂` for the Q0 turnkey major arc

This is the current “what remains” to fully eliminate the *project* major-arc boundary in the
turnkey route.

## Where the remaining axioms live

- `Goldbach/Cert/MajorArcModules/Q0TwoBoundsSpec.lean`
  - `q0Minor_ledger_engine` : the **ε₁** hypothesis (Q₀-complement / minor energy), in ledger form.
  - `q0Major_integrable` / `q0Major_small_bound` / `q0InnerMajor_large_beta` : the **ε₂** inputs
    (Q₀ major deviation), split into the β-splitting interfaces.

Sanity check (axiom audit):

- `lake env lean --stdin` with
  `#print axioms Goldbach.Cert.MajorArcEvalOnWindowCanonSpec.major_arc_eval_on_window_canon`
  currently reports the remaining project-specific axioms coming from `Q0TwoBoundsSpec` (plus
  standard classical/propext).

Everything downstream (including `Goldbach/Cert/TurnkeyMajorArcCanonSpec.lean` and
`Goldbach/Cert/MajorArcEvalOnWindowCanonSpec.lean`) now depends only on these analytic inputs plus
the `ℚ`-certificate checks (`Q0CertData.cert_valid`, and the auxiliary certs below).

## ε₁ (minor / Q₀-complement) — what we need to prove

Lean-facing target (current interface):

- `Goldbach/Cert/MajorArcModules/Q0MinorBound.lean`
  - `Q0MinorEnergyBound Δ E`:
    `∀ X ≥ X0, (∑ N ∈ EvenIn X H, ‖corr_integral X N - corr_integral_major_Q0 X N Δ‖^2) ≤ E`.

What is now *done* in Lean (bridge + certificate seam):

- `Goldbach/Cert/MajorArcModules/Q0MinorEngineSpec.lean`:
  - defines the bridge interface `Q0MinorEnergyEngine Δ E` stated in terms of
    `corr_integral_minor_Q0`, including the integrability hypotheses needed to identify
    `corr_integral - corr_integral_major_Q0 = corr_integral_minor_Q0`,
  - proves `Q0MinorEnergyEngine Δ E → Q0MinorEnergyBound Δ E`.

- `Goldbach/Cert/MajorArcModules/Q0MinorEnergyLedger.lean`:
  - defines the ledger-shaped engine interface
    `Q0MinorEnergyLedgerEngine Δ C2 C3` with bound
    `≤ C2*(H/X) + C3/(H*Q0^2)`,
  - proves monotonicity/constant propagation, and provides the certificate-facing lemma
    `engine16_of_ledger_of_E0_le`.

- `Goldbach/Cert/MajorArcModules/Q0MinorLedgerCert.lean`:
  - defines a generator-friendly `ℚ` certificate payload `{C2,C3}` and checks:
    `0 ≤ C2`, `0 ≤ C3`, and `C2/100 + C3/(9*10^12) ≤ 16`,
  - proves `Data.engine16_of_ledger_of_valid` as the glue:
    `(ledger engine) + (checked cap16) ⇒ Q0MinorEnergyEngine Δ 16`.

Notes alignment (Tenor / intended provenance of the missing analytic engine):

- `SupportingDocs/Goldbach tenor/sections/10_FinalAssembly.tex`
  - Variance ledger: `Var(S_{n0}) ≤ C2*(H/X)^{1/2} + C3/(H Q^2)`.
  - Attribution: SSU gives the `(H/X)^{1/2}` term; Type–I after alias suppression + AO gives the
    `1/(H Q^2)` term; “extracted statistic” stability is Lemma 7.21.

- `SupportingDocs/Goldbach tenor/sections/05b_SSU.tex`
  - SSU core: the single-tube inequality (labelled `thm:SSU-single`, displayed as (6.7)-style).
  - Global SSU: operator-norm bound `‖T‖_{2→2} ≪ (log X)^C (H/X)^{1/2}` (Sawyer/Cotlar–Stein route).

Missing in Lean today:

- No Lean formalization of the *analytic* SSU/alias-suppression/Type–I engine currently produces an
  inhabitant of `Q0MinorEnergyLedgerEngine Δ_canon C2 C3` (with explicit constants).

## ε₂ (Q₀ major deviation) — what we need to prove

Lean-facing target (current interface):

- `Goldbach/Cert/MajorArcModules/Q0MajorBound.lean`
  - `Q0MajorDeviationBound Δ ε₂`:
    `∀ X ≥ X0, ∀ N ∈ EvenIn X H, ‖corr_integral_major_Q0 X N Δ - corrModel N‖ ≤ ε₂`.

Available building blocks in Lean:

- BMOR-effective exponential-sum bounds (still depending on the BMOR ψ-axiom):
  - `Goldbach/Cert/ExplicitPNTinAP_BMOR.lean` (axiom input)
  - `Goldbach/Cert/MajorArcStep20BankExpSumApproxBMOR.lean`
  - `Goldbach/Cert/MajorArcStep21QuadraticIntegrandControlBMOR.lean`
  - Step 24 main-term identification:
    `Goldbach/Cert/MajorArcStep24AnalyticMainTerm.lean`
    and `Goldbach/Cert/MajorArcStep24MainTermIdentification.lean`

Missing in Lean today:

- No assembled lemma proving the `Q0MajorDeviationBound` interface from the Step 20/21/24 chain.
  The module layer `Goldbach/Cert/MajorArcModules/Q0MajorRoute.lean` records the intended split
  (integrable + small-β bound + large-β tail bound) but does not supply proofs.

## Certificate artifacts (numbers)

- `Goldbach/Cert/MajorArcModules/Q0CertData.lean` is generated by `scripts/gen_major_arc_q0_cert.py`
  and currently only certifies the *numeric cap* `ε₁+ε₂ ≤ 1014/125` in `ℚ`.

Additional (ε₁ ledger constants) numeric artifact:

- `Goldbach/Cert/MajorArcModules/Q0MinorLedgerCertData.lean` is generated by
  `scripts/gen_q0_minor_ledger_cert.py` and certifies the *cap16* inequality for the chosen
  ledger constants `C2,C3` (currently placeholders).

Additional (ε₂ large-β tail budgeting) numeric artifact:

- `Goldbach/Cert/MajorArcModules/Q0MajorTailCertData.lean` is generated by
  `scripts/gen_q0_major_tail_cert.py` and certifies the inequality
  `(2/51005) * M ≤ εl` (currently placeholders `M=50000`, `εl=2`), using the fixed lemma
  `kernelTailCap ≤ (2/51005 : ℝ)` from `Goldbach/Cert/MajorArcModules/KernelTailCapBounds.lean`.

Option 3 prep (TT*/Parseval reduction for proving the tail energy bound):

- `Goldbach/Cert/MajorArcModules/Q0MajorTailTTStar.lean` introduces
  `Q0InnerMajorLargeBetaTTStarBound Δ M2` and proves it implies the current tail interface
  `Q0MajorTailSpec.Q0InnerMajorLargeBetaEnergyBound Δ M2`.
  This isolates the remaining analytic task as a concrete Fourier-coefficient `ℓ²` bound.

Option 3 prep (mean-zero decomposition + deterministic remainder control):

- `Goldbach/Cert/MajorArcModules/Q0MajorTailMeanZero.lean` defines the mean-zero weight
  `majorArcWeightBalanced` and the decomposition
  `innerMajorQ0 = innerMajorQ0Balanced + innerMajorQ0Remainder`.
- `Goldbach/Cert/MajorArcModules/Q0MajorTailConstModeBound.lean` proves a crude deterministic bound
  on the constant mode `majorArcConstMode X Δ`.
- `Goldbach/Cert/MajorArcModules/Q0MajorTailRemainderBound.lean` gives a Fourier-polynomial
  description of `innerFull` and proves an `L²` bound (in β) for `innerFull` and hence
  `innerMajorQ0Remainder`.
- `Goldbach/Cert/MajorArcModules/Q0MajorTailWindows.lean` provides an explicit piecewise-linear
  window `wTextbook` that majorizes the hard arc indicator, for later use with weighted
  large-sieve-on-arcs lemmas.
- `Goldbach/Cert/MajorArcModules/Q0MajorTailAliasSuppressionSpec.lean` records (as an interface)
  the intended “hybrid large sieve + alias suppression” inequality against a *balanced* major-arc
  weight; this is the mathematician-facing payload needed to turn the TT*/Parseval bound into a
  proved theorem.

What is not yet generated:

- Any `ℚ`-certificate data that checks the analytic reductions needed for ε₁ or ε₂.
