import Goldbach.Cert.MajorArcModules.All
import Goldbach.Cert.MajorArcModules.Q0TwoBoundsSpec

/-!
Fool’s-gold convenience import: turnkey `Q0` instantiation.

This file exists so we can build the *current* pinned-cap `Q0` turnkey route on demand while
keeping the default build gold-grade.

It intentionally imports project-pinned axioms/budgets via `Q0TwoBoundsSpec.lean`.
-/

