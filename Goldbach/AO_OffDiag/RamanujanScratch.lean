/-!
Deprecated (historical scratch).

This file used to be a “from-scratch” skeleton for a fixed-`Q0` Ramanujan truncation story and a
tiny pinned σ-tail budget.

It is superseded by:
- `Goldbach/AO_OffDiag/TailBlock.lean` (the current fixed-`Q0` truncation definition and model API),
- `Goldbach/Cert/SigmaTailRealBoundFun.lean` (crude proved σ-tail bound used in FunX),
- `Goldbach/Cert/OffDiagBudgetAxiomsFun.lean` and `Goldbach/AO_OffDiag/TenorHypFunX_Canon.lean`
  (canonical `Q(X)` schedule + budget wiring).

The old “tiny pinned constant” discussion has been removed from this file to avoid confusion.
-/

import Mathlib
