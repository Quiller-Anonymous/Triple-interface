import SSU.Engines.LargeSieve.BGTubeToTubeData
import SSU.Engines.LargeSieve.BGTubeFiberLargeSieve

/-!
BG / `05_BG.tex` specializations for the SSU large-sieve layer.

This file is an import hub for deterministic plumbing that connects the explicit BG tube geometry
(`tubeFinset` in `SSU.Engines.BGTubeGeometry`) to the SSU Type–II large-sieve abstractions
(`SSU.Engines.TypeII.TubeData`) and the TeX-facing shear-box bookkeeping.

Nothing analytic happens here.
-/
