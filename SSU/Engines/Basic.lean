import SSU.Basic

namespace SSU
namespace Engines

/-!
Layer 3: analytic engines (deep).

This folder is for plugin-style lemmas that supply the *actual* interzone decay for a given
packetization (large sieve on arcs, dispersion, Type-I/II bounds, etc.). The core SSU library
should be usable without importing these.
-/

end SSU.Engines

