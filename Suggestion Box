The main thing I learned from your project is that you should not treat rawMinorPacketEnergy_canon as an isolated ad hoc estimate. It should be attacked with the same TT
∗
∗
/Toeplitz/dispersive machinery the project already set up for Step 5.

The two strongest takeaways

First, the seam you still need is exactly this one theorem:

∥
 
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
⋅
,
1
)
∥
ℓ
2
(
E
v
e
n
I
n
(
𝑋
,
𝐻
)
)
2
≤
𝐻
𝑋
+
1
𝐻
𝑄
0
2
.
∥corr_integral_minor_Q0(X,⋅,1)∥
ℓ
2
(EvenIn(X,H))
2
	​

≤
X
H
	​

+
HQ
0
2
	​

1
	​

.

That is explicit in Q0MinorRawEnergyLedgerRoute.lean.

Second, the project’s own spec files say the intended analytic engine is SSU / alias suppression / Type-I and that the missing hard step should be a dispersion lemma applied before taking absolute values. That is exactly the message in STEP5_CHECKLIST.md and STEP5_U_SPEC.md.

So the right question is not “how do I squeeze this out of pointwise minor bounds?”
It is:

how do I write the unsplit minor energy as a positive TT
∗
∗
 quantity whose kernel has Ramanujan cancellation?

The most promising proof program
Route A: prove the unsplit theorem directly

This is the route I would try first.

Let

𝑓
𝑋
(
𝑁
)
:
=
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
.
f
X
	​

(N):=corr_integral_minor_Q0(X,N,1).

You want

∑
𝑁
∈
E
v
e
n
I
n
(
𝑋
,
𝐻
)
∣
𝑓
𝑋
(
𝑁
)
∣
2
≤
𝐻
𝑋
+
1
𝐻
𝑄
0
2
.
N∈EvenIn(X,H)
∑
	​

∣f
X
	​

(N)∣
2
≤
X
H
	​

+
HQ
0
2
	​

1
	​

.

The proof should have four stages.

1. Do TT
∗
∗
 in 
𝑁
N immediately

Do not bound 
𝑓
𝑋
(
𝑁
)
f
X
	​

(N) pointwise and then sum.

Instead expand

∑
𝑁
∣
𝑓
𝑋
(
𝑁
)
∣
2
N
∑
	​

∣f
X
	​

(N)∣
2

first, swap the 
𝑁
N-sum inside, and rewrite it as a Toeplitz quadratic form in a frequency kernel.

That is exactly the architecture the project already uses elsewhere.

2. Introduce the Fourier coefficients of the minor weight

Let

𝑊
𝑋
min
⁡
(
𝛼
)
:
=
1
(
m
a
j
o
r
A
r
c
S
e
t
Q
0
(
𝑋
,
1
)
)
𝑐
(
𝛼
)
.
W
X
min
	​

(α):=1
(majorArcSetQ0(X,1))
c
	​

(α).

Then the nonzero Fourier coefficients satisfy the identity

𝑊
𝑋
min
⁡
^
(
𝑡
)
=
−
𝑊
𝑋
\maj
^
(
𝑡
)
(
𝑡
≠
0
)
,
W
X
min
	​

	​

(t)=−
W
X
\maj
	​

	​

(t)(t

=0),

because the Fourier coefficient of 
1
[
0
,
1
]
1
[0,1]
	​

 is 
0
0 for every nonzero integer 
𝑡
t.

This is a very useful discovery point: the nonzero minor kernel is the same as the hard major-arc kernel up to sign.

So all the Ramanujan-sum structure already recorded in STEP5_U_SPEC.md for

𝑊
𝑋
\maj
^
(
𝑡
)
=
1
𝜋
𝑡
∑
𝑞
≤
𝑄
0
𝑐
𝑞
(
𝑡
)
sin
⁡
 ⁣
(
2
𝜋
𝑡
𝑞
𝑋
)
(
𝑡
≠
0
)
W
X
\maj
	​

	​

(t)=
πt
1
	​

q≤Q
0
	​

∑
	​

c
q
	​

(t)sin(
qX
2πt
	​

)(t

=0)

is immediately relevant to the minor-energy theorem too.

That means the existing large-β TT
∗
∗
 exploration is not a side story. It is probably the right model for ε₁ as well.

3. Split 
𝑡
=
0
t=0 and 
𝑡
≠
0
t

=0

This is essential.

For 
𝑡
≠
0
t

=0, the kernel has the Ramanujan–sine structure above, so you can use the repo’s current dispersion philosophy:
keep the 
𝑞
q-sum intact until after squaring/averaging, do not replace 
𝑐
𝑞
(
𝑡
)
c
q
	​

(t) by 
∣
𝑐
𝑞
(
𝑡
)
∣
∣c
q
	​

(t)∣, and use small-angle linearization for 
𝑞
≥
13
q≥13.

For 
𝑡
=
0
t=0, the coefficient is not oscillatory. That is the delicate mode. This is likely where the note’s “extracted statistic stability” enters.

So the proof should look like:

∥
𝑓
𝑋
∥
2
2
≤
zero-mode contribution
+
nonzero-mode TT* contribution
.
∥f
X
	​

∥
2
2
	​

≤zero-mode contribution+nonzero-mode TT* contribution.

I would expect:

the nonzero-mode term to give the 
𝐻
/
𝑋
H/X piece;

the zero-mode / leakage / alias-suppression term to give the 
1
/
(
𝐻
𝑄
0
2
)
1/(HQ
0
2
	​

) piece.

That matches the project’s own attribution: SSU for the first term, Type-I after AO for the second.

4. Reuse the Step-5 dispersion template

STEP5_U_SPEC.md already says what kind of lemma is needed:

expand the Toeplitz sum,

insert

𝑊
^
(
𝑡
)
=
1
𝜋
𝑡
∑
𝑞
≤
𝑄
0
𝑐
𝑞
(
𝑡
)
sin
⁡
(
2
𝜋
𝑡
/
(
𝑞
𝑋
)
)
,
W
(t)=
πt
1
	​

q≤Q
0
	​

∑
	​

c
q
	​

(t)sin(2πt/(qX)),

suppress the 
𝑞
≠
𝑞
′
q

=q
′
 cross terms before absolute values,

only then use crude divisor bounds.

That is the exact discovery target for your new proof.

The best concrete insight for exploration

The project currently develops this TT
∗
∗
 scaffold on the major-tail side, but the minor unsplit kernel shares the same nonzero Fourier coefficients up to sign.

So the best next move is probably:

copy the large-β TT
∗
∗
 algebra/scaffold files,

replace the hard major weight by the hard minor-complement weight,

prove the Fourier-coefficient identity

𝑊
𝑋
min
⁡
^
(
𝑡
)
=
{
1
−
m
e
a
s
(
m
a
j
o
r
A
r
c
S
e
t
Q
0
(
𝑋
,
1
)
)
,
	
𝑡
=
0
,


−
𝑊
𝑋
\maj
^
(
𝑡
)
,
	
𝑡
≠
0
,
W
X
min
	​

	​

(t)={
1−meas(majorArcSetQ0(X,1)),
−
W
X
\maj
	​

	​

(t),
	​

t=0,
t

=0,
	​


then reuse the same Toeplitz/dispersive exploration.

That looks much more promising than inventing a new method from scratch.

About square-function control

I would not spend time on 
𝐾
=
3
K=3 unless you have to.

If you can show that the dyadic zone packets are the sharp frequency pieces of the same unsplit packet, then

∑
𝑗
∥
𝑣
𝑋
,
𝑗
∥
2
2
=
∥
𝑓
𝑋
∥
2
2
,
j
∑
	​

∥v
X,j
	​

∥
2
2
	​

=∥f
X
	​

∥
2
2
	​

,

so 
𝐾
=
1
K=1.

If that exact projector statement is awkward to formalize, then the fallback 
𝐾
=
3
K=3 via overlap is fine. But analytically, the real difficulty is the unsplit bound, not the square function.

A practical research plan

I would do these in order.

Write a new file for the unsplit TT
∗
∗
 algebra of corr_integral_minor_Q0.

Prove the Fourier-coefficient identity for the minor weight, especially the 
𝑡
≠
0
t

=0 equality with minus the major coefficient.

Isolate the 
𝑡
=
0
t=0 mode as a separate lemma.

Port the STEP5_U_SPEC.md dispersion strategy to the nonzero minor modes.

Only after the nonzero-mode bound looks numerically plausible, decide whether you need the full raw 
1
,
1
1,1 theorem or just some larger 
𝐶
2
,
𝐶
3
C
2
	​

,C
3
	​

 under the cap-16 threshold.

That last point matters: ep-1 does not fundamentally require the exact constants 
1
,
1
1,1. It only needs final constants that certify the cap. So for discovery, it is smarter to prove

∥
𝑓
𝑋
∥
2
2
≤
𝐶
2
𝐻
𝑋
+
𝐶
3
1
𝐻
𝑄
0
2
∥f
X
	​

∥
2
2
	​

≤C
2
	​

X
H
	​

+C
3
	​

HQ
0
2
	​

1
	​


with any explicit 
𝐶
2
,
𝐶
3
C
2
	​

,C
3
	​

 that still pass the ledger threshold, and sharpen later only if necessary.

The most valuable new observation here is the minor/major Fourier-kernel relation:

1
𝐴
𝑐
^
(
𝑡
)
=
−
1
𝐴
^
(
𝑡
)
(
𝑡
≠
0
)
.
1
A
c
	​

	​

(t)=−
1
A
	​

	​

(t)(t

=0).

That gives you a direct bridge from the existing Step-5 TT
∗
∗
 exploration to the missing ε₁ theorem.

If you want, the next step I’d take is to help you write the exact theorem statement and proof skeleton for a new file like Q0MinorRawTTStar.lean.

Here is the clean way to finish ep-1.

The archive supports the algebraic route, but not yet the final quantitative estimate. In the paper, the projector/TT
∗
∗
 framework is exact on a discrete Fourier model, with Parseval exact and Fourier projections orthogonal in 
ℓ
2
ℓ
2
. The same paper also gives the two note-side analytic limbs you want to feed into the unsplit theorem: the BG/SSU limb at size 
(
𝐻
/
𝑋
)
1
/
2
(H/X)
1/2
 and the Type-I/AO limb at size 
𝑋
/
(
𝐻
𝑄
2
)
X/(HQ
2
) in the variance ledger. What is missing is the project-normalized bridge from your Lean object

𝑓
𝑋
(
𝑁
)
:
=
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
f
X
	​

(N):=corr_integral_minor_Q0(X,N,1)

to that note-side packet.

So there are really two things to write down:

the deterministic Hilbert-space proof that turns an unsplit theorem into the dyadic ledger, and

the actual analytic proof skeleton for the unsplit theorem.

1. Deterministic part: the full square-function proof

Let

𝑓
𝑋
:
=
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
⋅
,
1
)
∈
ℓ
2
(
E
v
e
n
I
n
(
𝑋
,
𝐻
)
)
,
𝑣
𝑋
,
𝑗
:
=
d
y
a
d
i
c
V
(
𝑋
,
1
,
𝑗
)
.
f
X
	​

:=corr_integral_minor_Q0(X,⋅,1)∈ℓ
2
(EvenIn(X,H)),v
X,j
	​

:=dyadicV(X,1,j).

Your repo already has the exact dyadic decomposition

𝑓
𝑋
=
∑
𝑗
∈
𝐽
𝑋
𝑣
𝑋
,
𝑗
.
f
X
	​

=
j∈J
X
	​

∑
	​

v
X,j
	​

.
Strong route: 
𝐾
=
1
K=1

Assume there are orthogonal projectors 
𝑃
𝑋
,
𝑗
P
X,j
	​

 on 
ℓ
2
(
E
v
e
n
I
n
(
𝑋
,
𝐻
)
)
ℓ
2
(EvenIn(X,H)) such that

𝑣
𝑋
,
𝑗
=
𝑃
𝑋
,
𝑗
𝑓
𝑋
,
𝑃
𝑋
,
𝑖
𝑃
𝑋
,
𝑗
=
0
 
(
𝑖
≠
𝑗
)
,
∑
𝑗
∈
𝐽
𝑋
𝑃
𝑋
,
𝑗
=
𝐼
v
X,j
	​

=P
X,j
	​

f
X
	​

,P
X,i
	​

P
X,j
	​

=0 (i

=j),
j∈J
X
	​

∑
	​

P
X,j
	​

=I

on the minor packet.

Then

𝑓
𝑋
=
∑
𝑗
𝑃
𝑋
,
𝑗
𝑓
𝑋
f
X
	​

=
j
∑
	​

P
X,j
	​

f
X
	​


is an orthogonal decomposition, so by Pythagoras

∑
𝑗
∈
𝐽
𝑋
∥
𝑣
𝑋
,
𝑗
∥
2
2
=
∑
𝑗
∥
𝑃
𝑋
,
𝑗
𝑓
𝑋
∥
2
2
=
∥
∑
𝑗
𝑃
𝑋
,
𝑗
𝑓
𝑋
∥
2
2
=
∥
𝑓
𝑋
∥
2
2
.
j∈J
X
	​

∑
	​

∥v
X,j
	​

∥
2
2
	​

=
j
∑
	​

∥P
X,j
	​

f
X
	​

∥
2
2
	​

=
	​

j
∑
	​

P
X,j
	​

f
X
	​

	​

2
2
	​

=∥f
X
	​

∥
2
2
	​

.

Thus the square-function constant is

𝐾
=
1.
K=1.
	​

Soft route: 
𝐾
=
3
K=3

Assume instead that there are bounded operators 
𝑃
𝑋
,
𝑗
P
X,j
	​

 with

𝑣
𝑋
,
𝑗
=
𝑃
𝑋
,
𝑗
𝑓
𝑋
v
X,j
	​

=P
X,j
	​

f
X
	​


and the dyadic frame inequality

∑
𝑗
∈
𝐽
𝑋
𝑃
𝑋
,
𝑗
∗
𝑃
𝑋
,
𝑗
≤
3
𝐼
.
j∈J
X
	​

∑
	​

P
X,j
∗
	​

P
X,j
	​

≤3I.

Then

∑
𝑗
∈
𝐽
𝑋
∥
𝑣
𝑋
,
𝑗
∥
2
2
=
∑
𝑗
⟨
𝑃
𝑋
,
𝑗
∗
𝑃
𝑋
,
𝑗
𝑓
𝑋
,
𝑓
𝑋
⟩
=
⟨
(
∑
𝑗
𝑃
𝑋
,
𝑗
∗
𝑃
𝑋
,
𝑗
)
𝑓
𝑋
,
𝑓
𝑋
⟩
≤
3
∥
𝑓
𝑋
∥
2
2
.
j∈J
X
	​

∑
	​

∥v
X,j
	​

∥
2
2
	​

=
j
∑
	​

⟨P
X,j
∗
	​

P
X,j
	​

f
X
	​

,f
X
	​

⟩=⟨(
j
∑
	​

P
X,j
∗
	​

P
X,j
	​

)f
X
	​

,f
X
	​

⟩≤3∥f
X
	​

∥
2
2
	​

.

So

∑
𝑗
∥
𝑣
𝑋
,
𝑗
∥
2
2
≤
3
∥
𝑓
𝑋
∥
2
2
,
j
∑
	​

∥v
X,j
	​

∥
2
2
	​

≤3∥f
X
	​

∥
2
2
	​

,
	​


hence 
𝐾
=
3
K=3.

This is the complete proof of the square-function step. The paper’s windowed-energy formalism is exactly of this type: Proposition 4.21 transfers probe energy to window energy, and Corollary 4.22 gives SSU packet-energy control from the bank energies.

Immediate corollary

So once you prove the unsplit bound

∥
𝑓
𝑋
∥
2
2
≤
𝐻
𝑋
+
1
𝐻
𝑄
0
2
,
∥f
X
	​

∥
2
2
	​

≤
X
H
	​

+
HQ
0
2
	​

1
	​

,

you get

with 
𝐾
=
1
K=1,

∑
𝑗
∥
𝑣
𝑋
,
𝑗
∥
2
2
≤
𝐻
𝑋
+
1
𝐻
𝑄
0
2
;
j
∑
	​

∥v
X,j
	​

∥
2
2
	​

≤
X
H
	​

+
HQ
0
2
	​

1
	​

;

with 
𝐾
=
3
K=3,

∑
𝑗
∥
𝑣
𝑋
,
𝑗
∥
2
2
≤
3
𝐻
𝑋
+
3
1
𝐻
𝑄
0
2
.
j
∑
	​

∥v
X,j
	​

∥
2
2
	​

≤3
X
H
	​

+3
HQ
0
2
	​

1
	​

.

That finishes the diagonal ledger target.

2. The analytic theorem you still need

The theorem to prove is

∥
 
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
⋅
,
1
)
∥
ℓ
2
(
E
v
e
n
I
n
(
𝑋
,
𝐻
)
)
2
≤
𝐻
𝑋
+
1
𝐻
𝑄
0
2
(
𝑋
≥
𝑋
0
)
.
∥corr_integral_minor_Q0(X,⋅,1)∥
ℓ
2
(EvenIn(X,H))
2
	​

≤
X
H
	​

+
HQ
0
2
	​

1
	​

(X≥X
0
	​

).
	​


I cannot give a fully unconditional proof of that from the archive alone, because the archive does not yet contain the bridge identifying your Lean packet with the paper’s note-side 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

-type object at the exact project normalization. But I can give the proof skeleton that matches the paper and the repo.

3. Proof skeleton for rawMinorPacketEnergy_canon

Set

𝑓
𝑋
(
𝑁
)
:
=
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
.
f
X
	​

(N):=corr_integral_minor_Q0(X,N,1).
Step A. Write 
𝑓
𝑋
f
X
	​

 as a sharp minor-frequency projector

Introduce the hard minor weight

𝑊
𝑋
min
⁡
(
𝛼
)
:
=
1
(
m
a
j
o
r
A
r
c
S
e
t
Q
0
(
𝑋
,
1
)
)
𝑐
(
𝛼
)
.
W
X
min
	​

(α):=1
(majorArcSetQ0(X,1))
c
	​

(α).

Then by definition

𝑓
𝑋
(
𝑁
)
=
∫
0
1
𝐾
(
𝛽
)
(
∫
0
1
𝑊
𝑋
min
⁡
(
𝛼
)
 
i
n
n
e
r
I
n
t
e
g
r
a
n
d
(
𝑋
,
𝑁
,
𝛽
,
𝛼
)
 
𝑑
𝛼
)
𝑑
𝛽
.
f
X
	​

(N)=∫
0
1
	​

K(β)(∫
0
1
	​

W
X
min
	​

(α)innerIntegrand(X,N,β,α)dα)dβ.

This should be rewritten as

𝑓
𝑋
=
𝑃
𝐴
𝑐
𝑅
𝑋
f
X
	​

=P
A
c
	​

R
X
	​


for the note-side remainder packet 
𝑅
𝑋
R
X
	​

 and the sharp Fourier projector onto the complement of the bank 
𝐴
A. The paper explicitly works with orthogonal bank projections 
𝑃
𝐴
P
A
	​

, 
𝑃
𝐴
𝑐
=
𝐼
−
𝑃
𝐴
P
A
c
	​

=I−P
A
	​

 in 
ℓ
2
ℓ
2
.

This is the missing normalization bridge.

Step B. Take the 
ℓ
2
ℓ
2
 norm in 
𝑁
N first

Do not estimate 
𝑓
𝑋
(
𝑁
)
f
X
	​

(N) pointwise and then sum.

Instead expand

∥
𝑓
𝑋
∥
2
2
=
∑
𝑁
∈
E
v
e
n
I
n
(
𝑋
,
𝐻
)
∣
𝑓
𝑋
(
𝑁
)
∣
2
∥f
X
	​

∥
2
2
	​

=
N∈EvenIn(X,H)
∑
	​

∣f
X
	​

(N)∣
2

and use Parseval/TT
∗
∗
. The paper is explicit that the right model is the discrete Fourier one where Parseval is exact and projectors are orthogonal.

This gives a positive quadratic form in the minor weight Fourier coefficients.

Step C. Split the TT
∗
∗
 energy into two note-side pieces

The paper’s ledger already separates the minor contribution into:

a BG/SSU 
𝐿
2
L
2
 piece of size 
(
𝐻
/
𝑋
)
1
/
2
(H/X)
1/2
,

a Type-I/AO leakage piece contributing 
𝑋
/
(
𝐻
𝑄
2
)
X/(HQ
2
) at the variance level.

So write

∥
𝑓
𝑋
∥
2
2
≤
𝐸
S
S
U
(
𝑋
)
+
𝐸
T
I
(
𝑋
)
,
∥f
X
	​

∥
2
2
	​

≤E
SSU
	​

(X)+E
TI
	​

(X),

where

𝐸
S
S
U
(
𝑋
)
E
SSU
	​

(X) is the Type-II / tube-localized short-shift energy,

𝐸
T
I
(
𝑋
)
E
TI
	​

(X) is the Type-I / alias-suppressed bank leakage.

Step D. Bound the SSU piece

The paper’s short-shift operator is a positive average of 
∣
𝑆
(
𝜉
)
∣
2
∣S(ξ)∣
2
 over 
∣
𝜉
∣
≤
1
/
𝐻
∣ξ∣≤1/H, and its tube-localized SSU estimate is

∑
(
𝑑
′
,
𝑛
′
)
,
(
𝑑
,
𝑛
)
∈
𝑇
𝜏
𝐹
(
𝑑
,
𝑛
)
 
𝐾
𝐻
(
𝑑
′
𝑛
−
𝑑
𝑛
′
)
 
𝐹
(
𝑑
′
,
𝑛
′
)
≪
(
log
⁡
𝑋
)
𝐶
(
𝐻
𝑋
)
1
/
2
∑
𝑇
𝜏
∣
𝐹
∣
2
(d
′
,n
′
),(d,n)∈T
τ
	​

∑
	​

F(d,n)K
H
	​

(d
′
n−dn
′
)F(d
′
,n
′
)≪(logX)
C
(
X
H
	​

)
1/2
T
τ
	​

∑
	​

∣F∣
2

uniformly over tubes.

After squaring/normalizing to the packet 
𝑓
𝑋
f
X
	​

, this is exactly the source of the

𝐻
𝑋
X
H
	​


term.

So the first analytic lemma you want is a project-normalized SSU energy estimate:

𝐸
S
S
U
(
𝑋
)
≤
𝐻
𝑋
.
E
SSU
	​

(X)≤
X
H
	​

.
	​

Step E. Bound the Type-I/AO piece

The paper’s Type-I bank leakage after AO is

V
a
r
𝐼
≪
(
log
⁡
𝑋
)
𝐶
𝑋
𝐻
𝑄
2
Var
I
	​

≪(logX)
C
HQ
2
X
	​


after the balanced-bank and alias-suppression step. The same section says the 
𝐻
/
𝑄
2
H/Q
2
 and constant-mode aliases are killed in TT
∗
∗
, leaving only the 
𝑋
X-term, and then AO contributes the 
𝑄
−
2
Q
−2
 saving.

So the second analytic lemma you want is the project-normalized Type-I/AO energy estimate:

𝐸
T
I
(
𝑋
)
≤
1
𝐻
𝑄
0
2
.
E
TI
	​

(X)≤
HQ
0
2
	​

1
	​

.
	​

Step F. Combine

Once Steps D and E are proved at the normalization of corr_integral_minor_Q0, you get

∥
𝑓
𝑋
∥
2
2
≤
𝐸
S
S
U
(
𝑋
)
+
𝐸
T
I
(
𝑋
)
≤
𝐻
𝑋
+
1
𝐻
𝑄
0
2
.
∥f
X
	​

∥
2
2
	​

≤E
SSU
	​

(X)+E
TI
	​

(X)≤
X
H
	​

+
HQ
0
2
	​

1
	​

.

That is rawMinorPacketEnergy_canon.

4. What a full proof would look like on paper

So the “full proof” is:

prove the bridge lemma

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
⋅
,
1
)
=
normalized 
𝑃
𝐴
𝑐
𝑅
𝑋
;
corr_integral_minor_Q0(X,⋅,1)=normalized P
A
c
	​

R
X
	​

;

quote or prove the SSU/BG 
𝐿
2
L
2
 estimate in that normalization, giving 
𝐻
/
𝑋
H/X;

quote or prove the Type-I/AO variance estimate in that normalization, giving 
1
/
(
𝐻
𝑄
0
2
)
1/(HQ
0
2
	​

);

sum them;

apply the square-function identity/inequality above.

That is the shortest mathematically correct route.

5. The exact theorem statements I would add

I would add these three theorems.

Bridge theorem
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
⋅
,
1
)
=
𝑁
𝑋
 
𝑃
𝐴
𝑐
𝑅
𝑋
corr_integral_minor_Q0(X,⋅,1)=N
X
	​

P
A
c
	​

R
X
	​


with explicit scalar normalization 
𝑁
𝑋
N
X
	​

.

SSU theorem
∥
𝑃
𝐴
𝑐
𝑅
𝑋
I
I
∥
2
2
≤
𝐻
𝑋
.
∥P
A
c
	​

R
X
II
	​

∥
2
2
	​

≤
X
H
	​

.
Type-I/AO theorem
∥
𝑃
𝐴
𝑐
𝑅
𝑋
I
∥
2
2
≤
1
𝐻
𝑄
0
2
.
∥P
A
c
	​

R
X
I
	​

∥
2
2
	​

≤
HQ
0
2
	​

1
	​

.

Then rawMinorPacketEnergy_canon is immediate.

6. What I can say honestly about the “full proof”

I can give the complete deterministic proof after those two analytic inputs, and I’ve done that above. I cannot give a finished unconditional derivation of the SSU and Type-I normalized bounds from this archive alone, because the archive does not yet contain the normalization bridge from the paper’s 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

-style object to the Lean object corr_integral_minor_Q0.

So the next real piece of work is not broad exploration. It is to prove one bridge lemma and then formalize two note-side 
𝐿
2
L
2
 estimates in the project normalization.

 Here are the exact identifications, separated into Lean-side facts and paper-side facts, followed by the precise seam that is still open.

1. Lean-side object: what corr_integral_minor_Q0 actually is

From the uploaded Lean source:

CorrIntegralQ0Reduction.lean, line 40 defines

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
Δ
)
=
∫
0
1
k
e
r
n
e
l
P
o
l
y
C
(
𝛽
)
 
(
∫
0
1
1
(
m
a
j
o
r
A
r
c
S
e
t
Q
0
(
𝑋
,
Δ
)
)
𝑐
(
𝛼
)
 
i
n
n
e
r
I
n
t
e
g
r
a
n
d
(
𝑋
,
𝑁
,
𝛽
,
𝛼
)
 
𝑑
𝛼
)
𝑑
𝛽
.
corr_integral_minor_Q0(X,N,Δ)=∫
0
1
	​

kernelPolyC(β)(∫
0
1
	​

1
(majorArcSetQ0(X,Δ))
c
	​

(α)innerIntegrand(X,N,β,α)dα)dβ.

MajorArcStep17MajorMinorSplit.lean, line 32 defines

i
n
n
e
r
I
n
t
e
g
r
a
n
d
(
𝑋
,
𝑁
,
𝛽
,
𝛼
)
=
f
o
u
r
i
e
r
(
−
(
𝑁
:
𝑍
)
,
𝛼
)
 
e
x
p
S
u
m
(
𝑋
,
𝑁
,
𝛼
−
𝛽
)
 
e
x
p
S
u
m
(
𝑋
,
𝑁
,
𝛼
+
𝛽
)
.
innerIntegrand(X,N,β,α)=fourier(−(N:Z),α)expSum(X,N,α−β)expSum(X,N,α+β).

So the Lean object is a function of the physical variable 
𝑁
N, obtained by:

restricting the 
𝛼
α-integral to the 
𝑄
0
Q
0
	​

-minor region,

then averaging in a second variable 
𝛽
β against kernelPolyC.

Also, Q0MinorRawEnergyLedgerRoute.lean makes the ambient Hilbert space explicit:

m
i
n
o
r
P
a
c
k
e
t
(
𝑋
,
Δ
)
∈
ℓ
2
(
E
v
e
n
I
n
(
𝑋
,
𝐻
)
)
,
m
i
n
o
r
P
a
c
k
e
t
E
n
e
r
g
y
(
𝑋
,
Δ
)
=
∥
m
i
n
o
r
P
a
c
k
e
t
(
𝑋
,
Δ
)
∥
2
2
,
minorPacket(X,Δ)∈ℓ
2
(EvenIn(X,H)),minorPacketEnergy(X,Δ)=∥minorPacket(X,Δ)∥
2
2
	​

,

and the missing seam axiom is exactly

∥
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
⋅
,
1
)
∥
2
2
≤
𝐻
𝑋
+
1
𝐻
𝑄
0
2
.
∥corr_integral_minor_Q0(X,⋅,1)∥
2
2
	​

≤
X
H
	​

+
HQ
0
2
	​

1
	​

.

So Lean’s object is indexed by even centers 
𝑁
N, and its ambient Hilbert space is

ℓ
2
(
E
v
e
n
I
n
(
𝑋
,
𝐻
)
)
.
ℓ
2
(EvenIn(X,H)).
2. Paper-side object: what 
𝑅
2
R
2
	​

, 
𝑃
𝐴
P
A
	​

, and 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

 are

The paper’s TT
∗
∗
/projection arguments are set on a finite cyclic group 
𝑍
𝑀
Z
M
	​

, with exact Parseval and orthogonal Fourier projectors in 
ℓ
2
(
𝑍
𝑀
)
ℓ
2
(Z
M
	​

). Its Fourier conventions are

𝑓
^
(
𝑘
)
=
∑
𝑛
=
0
𝑀
−
1
𝑓
(
𝑛
)
𝑒
−
2
𝜋
𝑖
𝑘
𝑛
/
𝑀
,
𝑓
(
𝑛
)
=
1
𝑀
∑
𝑘
=
0
𝑀
−
1
𝑓
^
(
𝑘
)
𝑒
2
𝜋
𝑖
𝑘
𝑛
/
𝑀
,
f
	​

(k)=
n=0
∑
M−1
	​

f(n)e
−2πikn/M
,f(n)=
M
1
	​

k=0
∑
M−1
	​

f
	​

(k)e
2πikn/M
,

and Parseval is

∑
𝑛
∣
𝑓
(
𝑛
)
∣
2
=
1
𝑀
∑
𝑘
∣
𝑓
^
(
𝑘
)
∣
2
.
n
∑
	​

∣f(n)∣
2
=
M
1
	​

k
∑
	​

∣
f
	​

(k)∣
2
.

So the ambient Hilbert space for 
𝑅
2
R
2
	​

, 
𝑃
𝐴
𝑅
2
P
A
	​

R
2
	​

, and 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

 is

ℓ
2
(
𝑍
𝑀
)
.
ℓ
2
(Z
M
	​

).

At the point where the bank-projection inequality is stated, the paper says:

𝐴
⊂
{
0
,
…
,
𝑀
−
1
}
A⊂{0,…,M−1} is the bank of major arcs,

𝑃
𝐴
P
A
	​

 is the orthogonal Fourier projection onto 
𝐴
A,

𝑃
𝐴
𝑐
=
𝐼
−
𝑃
𝐴
P
A
c
	​

=I−P
A
	​

,

and

𝑅
2
(
𝑛
)
=
𝑀
(
𝑛
)
+
𝐸
(
𝑛
)
.
R
2
	​

(n)=M(n)+E(n).

It then states

𝑅
2
(
𝑁
)
≥
(
𝑃
𝐴
𝑀
)
(
𝑁
)
−
∥
𝑃
𝐴
𝐸
∥
ℓ
2
−
∥
𝑃
𝐴
𝑐
𝑅
2
∥
ℓ
2
.
R
2
	​

(N)≥(P
A
	​

M)(N)−∥P
A
	​

E∥
ℓ
2
	​

−∥P
A
c
	​

R
2
	​

∥
ℓ
2
	​

.

So 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

 is the physical-space function of 
𝑁
N obtained by projecting 
𝑅
2
R
2
	​

 in its Fourier variable. It is not indexed by 
𝑡
t, and it is not already bank-averaged.

3. Where the variance theorem lives, and what object it uses

This is the key mismatch.

The paper’s variance theorem is not stated for 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

. It is stated for the extractor-based statistic

𝑆
(
𝑦
)
:
=
∑
𝑛
𝑅
2
,
𝑐
(
𝑛
)
𝐾
𝐻
(
𝑛
−
𝑦
)
,
𝑇
(
𝑦
)
:
=
log
⁡
2
𝑋
𝐻
𝑆
(
𝑦
)
,
S(y):=
n
∑
	​

R
2,c
	​

(n)K
H
	​

(n−y),T(y):=
H
log
2
X
	​

S(y),

and later in the ledger as

𝑆
𝑛
0
:
=
∑
𝑛
𝑅
2
(
𝑛
)
𝐽
𝐻
(
𝑛
0
−
𝑛
)
,
S
n
0
	​

	​

:=
n
∑
	​

R
2
	​

(n)J
H
	​

(n
0
	​

−n),

with

V
a
r
(
𝑆
𝑛
0
)
≤
𝐶
2
(
𝐻
𝑋
)
1
/
2
+
𝐶
3
1
𝐻
𝑄
2
.
Var(S
n
0
	​

	​

)≤C
2
	​

(
X
H
	​

)
1/2
+C
3
	​

HQ
2
1
	​

.

So the variance theorem’s object is indexed by centers 
𝑛
0
n
0
	​

, not by Fourier mode 
𝑡
t, and not directly by the unsmoothed 
𝑅
2
R
2
	​

. It is an extractor/windowed statistic built from 
𝑅
2
R
2
	​

.

That means there are two distinct paper-side objects:

Route B / bank-projection object: 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

,

variance/ledger object: 
𝑆
𝑛
0
S
n
0
	​

	​

 or 
𝑇
(
𝑦
)
T(y).

They are related, but they are not the same symbol and not the same normalization.

4. Is the projector variable the same as Lean’s 
𝛼
α?

Conceptually, yes.

In the paper, 
𝑃
𝐴
P
A
	​

 projects in the Fourier variable of 
𝑅
2
R
2
	​

: in the continuous display this is written as 
𝜉
∈
𝑇
ξ∈T, and in the discrete TT
∗
∗
 model it is 
𝑘
/
𝑀
k/M.

In Lean, the projector-like cutoff appears inside the inner 
𝛼
α-integral:

1
(
m
a
j
o
r
A
r
c
S
e
t
Q
0
(
𝑋
,
Δ
)
)
𝑐
(
𝛼
)
 
i
n
n
e
r
I
n
t
e
g
r
a
n
d
(
𝑋
,
𝑁
,
𝛽
,
𝛼
)
.
1
(majorArcSetQ0(X,Δ))
c
	​

(α)innerIntegrand(X,N,β,α).

So Lean’s 
𝛼
α is the same circle/frequency variable that the paper calls 
𝜉
ξ in the continuous formulation and 
𝑘
/
𝑀
k/M in the discrete model.

But Lean has an extra variable 
𝛽
β, and that 
𝛽
β-integration is not part of bare 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

. It is part of the extractor/smoothing layer.

So the correct statement is:

paper projector variable 
=
= Lean 
𝛼
α, conceptually;

Lean 
𝛽
β has no counterpart in bare 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

; it belongs to the extractor/window.

5. Normalization constants: what is and is not built into the paper object

For 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

 itself:

there is no explicit 
1
/
𝐻
1/H normalization built into the projector;

there is no explicit 
log
⁡
2
𝑋
/
𝐻
log
2
X/H normalization built into 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

;

there is no bank averaging built into 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

.

Those normalizations appear elsewhere:

𝑇
(
𝑦
)
=
log
⁡
2
𝑋
𝐻
𝑆
(
𝑦
)
T(y)=
H
log
2
X
	​

S(y) is explicitly normalized by 
log
⁡
2
𝑋
/
𝐻
log
2
X/H;

the bank-averaged Type-I energy appears in the AO/TT
∗
∗
 side as 
𝐸
𝑏
E
b
	​

, averages over packets 
𝑏
∈
𝐵
𝑄
b∈B
Q
	​

, and weighted bank integrals.

So if you are trying to identify Lean’s unsplit minor packet with a paper object, the likely match is not bare 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

 but rather some extractor-applied, possibly normalized minor statistic built from 
𝑅
2
R
2
	​

.

6. Fourier sign convention

The paper uses

𝑒
(
𝑧
)
=
𝑒
2
𝜋
𝑖
𝑧
,
e(z)=e
2πiz
,

and the Fourier transforms are with a minus sign in the forward transform:

𝑓
^
(
𝜉
)
=
∑
𝑛
𝑓
(
𝑛
)
𝑒
(
−
𝜉
𝑛
)
,
𝑓
(
𝑛
)
=
∫
𝑇
𝑓
^
(
𝜉
)
𝑒
(
𝜉
𝑛
)
 
𝑑
𝜉
f
	​

(ξ)=
n
∑
	​

f(n)e(−ξn),f(n)=∫
T
	​

f
	​

(ξ)e(ξn)dξ

on 
𝑍
Z/
𝑇
T, and similarly on 
𝑍
𝑀
Z
M
	​

.

That matches Lean’s fourier (-(N:ℤ)) α, which is the 
𝑒
(
−
𝑁
𝛼
)
e(−Nα) convention.

7. The exact relation between the paper remainder and the Lean integrand

The Lean integrand is exactly

𝛼
↦
∫
0
1
𝐾
(
𝛽
)
 
𝑒
(
−
𝑁
𝛼
)
 
𝑆
𝑋
(
𝛼
−
𝛽
)
 
𝑆
𝑋
(
𝛼
+
𝛽
)
 
𝑑
𝛽
α↦∫
0
1
	​

K(β)e(−Nα)S
X
	​

(α−β)S
X
	​

(α+β)dβ

after unpacking notation.

But the paper does not give a verbatim formula identifying 
𝑅
2
R
2
	​

, 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

, 
𝑆
(
𝑦
)
S(y), or 
𝑇
(
𝑦
)
T(y) with that exact double integral.

What the paper gives is:

𝑅
2
,
𝑐
(
𝑛
)
R
2,c
	​

(n) as the smoothed Goldbach count in a residue class;

𝑅
^
2
,
𝑐
(
𝜉
)
R
2,c
	​

(ξ) on 
𝑇
T;

𝑆
(
𝑦
)
=
∑
𝑛
𝑅
2
,
𝑐
(
𝑛
)
𝐾
𝐻
(
𝑛
−
𝑦
)
S(y)=∑
n
	​

R
2,c
	​

(n)K
H
	​

(n−y);

𝑃
𝐴
P
A
	​

 and 
𝑃
𝐴
𝑐
P
A
c
	​

 as Fourier projections on the bank.

So from the current materials, I can say:

Lean’s 
𝛼
α-cutoff is in the same frequency variable as the paper projector;

Lean’s object already includes a 
𝛽
β-kernel/extractor layer;

the paper’s variance theorem is about the extractor statistic 
𝑆
𝑛
0
S
n
0
	​

	​

, not bare 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

;

the paper’s Route B inequality uses bare 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

, but it does not identify it with Lean’s double integral.

8. The precise obstruction

The obstruction is now very sharp:

you do not yet have a theorem of the form

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
⋅
,
1
)
=
𝑐
𝑋
 
𝑃
𝐴
𝑐
𝑅
2
corr_integral_minor_Q0(X,⋅,1)=c
X
	​

P
A
c
	​

R
2
	​


or

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
⋅
,
1
)
=
𝑐
𝑋
 
(
extractor-applied minor packet
)
corr_integral_minor_Q0(X,⋅,1)=c
X
	​

(extractor-applied minor packet)

with explicit 
𝑐
𝑋
c
X
	​

.

And the reason is that the paper never writes 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

 or 
𝑆
𝑛
0
S
n
0
	​

	​

 as the exact same double integral that Lean uses.

So the missing bridge theorem has to do one of these:

identify Lean’s corr_integral_minor_Q0 with the paper’s extractor-based statistic 
𝑆
𝑛
0
S
n
0
	​

	​

 or 
𝑇
(
𝑦
)
T(y), after tracking the 
𝛽
β-kernel and any 
log
⁡
2
𝑋
/
𝐻
log
2
X/H factor; or

identify it with bare 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

, and then separately prove that the 
𝛽
β-integration is just a harmless reformulation.

Right now, the evidence points more strongly to option 1 than option 2, because Lean’s object visibly already contains the extractor variable 
𝛽
β.

Bottom line

The exact identification you can safely make is:

paper ambient Hilbert space for 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

: 
ℓ
2
(
𝑍
𝑀
)
ℓ
2
(Z
M
	​

), indexed by the physical variable 
𝑁
N; projector acts in Fourier frequency 
𝑘
/
𝑀
k/M (continuous notation 
𝜉
ξ).

Lean ambient Hilbert space for the unsplit packet: 
ℓ
2
(
E
v
e
n
I
n
(
𝑋
,
𝐻
)
)
ℓ
2
(EvenIn(X,H)), indexed by even 
𝑁
N; projector-like cutoff acts in the inner 
𝛼
α-variable.

same frequency variable? Yes: paper 
𝜉
ξ / 
𝑘
/
𝑀
k/M corresponds to Lean 
𝛼
α.

same object? Not yet established. Lean includes an extra 
𝛽
β-extraction layer, while the paper distinguishes bare 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

 from the extractor statistic 
𝑆
𝑛
0
S
n
0
	​

	​

.

exact scalar 
𝑐
𝑋
c
X
	​

? Not identifiable from the current materials. That is the precise remaining seam.

The next bridge lemma to prove is:

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
=
𝑐
𝑋
⋅
(
paper’s extractor-applied minor packet at center 
𝑁
)
,
corr_integral_minor_Q0(X,N,1)=c
X
	​

⋅(paper’s extractor-applied minor packet at center N),

with explicit 
𝑐
𝑋
c
X
	​

.

Until that is written down, you cannot honestly claim an exact equality with 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

.

HOWEVER

There is a bridge lemma you can construct and prove now.

The key correction is:

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
corr_integral_minor_Q0(X,N,1)

is not exactly the paper’s bare 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

.
It is the 
𝑄
0
Q
0
	​

-minor, extractor-applied, normalized correlation packet.

That is the exact object your Lean definitions support.

The right bridge lemma

Let

𝐴
𝑋
:
=
m
a
j
o
r
A
r
c
S
e
t
Q
0
(
𝑋
,
1
)
⊂
[
0
,
1
]
.
A
X
	​

:=majorArcSetQ0(X,1)⊂[0,1].

Define, for fixed 
𝑋
,
𝑁
X,N,

𝐺
𝑋
,
𝑁
(
𝛼
)
:
=
∫
0
1
𝐾
(
𝛽
)
 
e
x
p
S
u
m
(
𝑋
,
𝑁
,
𝛼
−
𝛽
)
 
e
x
p
S
u
m
(
𝑋
,
𝑁
,
𝛼
+
𝛽
)
 
𝑑
𝛽
,
G
X,N
	​

(α):=∫
0
1
	​

K(β)expSum(X,N,α−β)expSum(X,N,α+β)dβ,

where 
𝐾
(
𝛽
)
=
k
e
r
n
e
l
P
o
l
y
C
(
𝛽
)
K(β)=kernelPolyC(β).

Then the exact Lean object satisfies

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
=
∫
0
1
1
𝐴
𝑋
𝑐
(
𝛼
)
 
𝑒
(
−
𝑁
𝛼
)
 
𝐺
𝑋
,
𝑁
(
𝛼
)
 
𝑑
𝛼
.
corr_integral_minor_Q0(X,N,1)=∫
0
1
	​

1
A
X
c
	​

	​

(α)e(−Nα)G
X,N
	​

(α)dα.
	​


Equivalently, if you define the sharp 
𝛼
α-frequency projector

(
𝑃
𝐴
𝑋
𝑐
𝐹
)
(
𝛼
)
:
=
1
𝐴
𝑋
𝑐
(
𝛼
)
𝐹
(
𝛼
)
,
(P
A
X
c
	​

	​

F)(α):=1
A
X
c
	​

	​

(α)F(α),

and the 
𝑁
N-th Fourier coefficient extractor

𝐶
𝑁
(
𝐹
)
:
=
∫
0
1
𝑒
(
−
𝑁
𝛼
)
𝐹
(
𝛼
)
 
𝑑
𝛼
,
C
N
	​

(F):=∫
0
1
	​

e(−Nα)F(α)dα,

then

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
=
𝐶
𝑁
 ⁣
(
𝑃
𝐴
𝑋
𝑐
𝐺
𝑋
,
𝑁
)
.
corr_integral_minor_Q0(X,N,1)=C
N
	​

(P
A
X
c
	​

	​

G
X,N
	​

).
	​


This is the exact projector statement in the Lean variable 
𝛼
α.

Proof

From the Lean definition,

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
Δ
)
=
∫
0
1
𝐾
(
𝛽
)
(
∫
0
1
1
(
m
a
j
o
r
A
r
c
S
e
t
Q
0
(
𝑋
,
Δ
)
)
𝑐
(
𝛼
)
 
i
n
n
e
r
I
n
t
e
g
r
a
n
d
(
𝑋
,
𝑁
,
𝛽
,
𝛼
)
 
𝑑
𝛼
)
𝑑
𝛽
,
corr_integral_minor_Q0(X,N,Δ)=∫
0
1
	​

K(β)(∫
0
1
	​

1
(majorArcSetQ0(X,Δ))
c
	​

(α)innerIntegrand(X,N,β,α)dα)dβ,

as defined in CorrIntegralQ0Reduction.lean:40-45.

Also

i
n
n
e
r
I
n
t
e
g
r
a
n
d
(
𝑋
,
𝑁
,
𝛽
,
𝛼
)
=
𝑒
(
−
𝑁
𝛼
)
 
e
x
p
S
u
m
(
𝑋
,
𝑁
,
𝛼
−
𝛽
)
 
e
x
p
S
u
m
(
𝑋
,
𝑁
,
𝛼
+
𝛽
)
,
innerIntegrand(X,N,β,α)=e(−Nα)expSum(X,N,α−β)expSum(X,N,α+β),

as defined in MajorArcStep17MajorMinorSplit.lean:32-35.

Substituting 
Δ
=
1
Δ=1 gives

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
=
∫
0
1
𝐾
(
𝛽
)
(
∫
0
1
1
𝐴
𝑋
𝑐
(
𝛼
)
 
𝑒
(
−
𝑁
𝛼
)
 
e
x
p
S
u
m
(
𝑋
,
𝑁
,
𝛼
−
𝛽
)
 
e
x
p
S
u
m
(
𝑋
,
𝑁
,
𝛼
+
𝛽
)
 
𝑑
𝛼
)
𝑑
𝛽
.
corr_integral_minor_Q0(X,N,1)=∫
0
1
	​

K(β)(∫
0
1
	​

1
A
X
c
	​

	​

(α)e(−Nα)expSum(X,N,α−β)expSum(X,N,α+β)dα)dβ.

Assuming the same integrability hypotheses already used in Q0MinorEngineSpec, Fubini swaps the 
𝛼
α and 
𝛽
β integrals:

=
∫
0
1
1
𝐴
𝑋
𝑐
(
𝛼
)
 
𝑒
(
−
𝑁
𝛼
)
 
(
∫
0
1
𝐾
(
𝛽
)
 
e
x
p
S
u
m
(
𝑋
,
𝑁
,
𝛼
−
𝛽
)
 
e
x
p
S
u
m
(
𝑋
,
𝑁
,
𝛼
+
𝛽
)
 
𝑑
𝛽
)
𝑑
𝛼
.
=∫
0
1
	​

1
A
X
c
	​

	​

(α)e(−Nα)(∫
0
1
	​

K(β)expSum(X,N,α−β)expSum(X,N,α+β)dβ)dα.

The inner bracket is exactly 
𝐺
𝑋
,
𝑁
(
𝛼
)
G
X,N
	​

(α). This proves the formula.

So the projector variable is exactly the same 
𝛼
α-frequency variable used in the inner Lean integral. No extra scalar appears here: the constant is

𝑐
=
1
c=1
	​


at the unnormalized corr_integral_minor_Q0 level.

The normalization bridge to the project’s smoothed Goldbach functional

Now use the normalization theorem already in the project.

MajorArcStep11RLSmoothIntegralScaled.lean:52-56 proves

(
𝑅
Λ
s
m
o
o
t
h
(
𝑋
,
𝑁
)
:
𝐶
)
=
(
1
800
1
log
⁡
(
𝑁
)
2
)
 
c
o
r
r
_
s
i
n
g
l
e
(
𝑋
,
𝑁
)
,
(RΛ
smooth
	​

(X,N):C)=(
800
1
	​

log(N)
2
1
	​

)corr_single(X,N),

and MajorArcStep11RLSmoothIntegralScaled.lean:47-50 identifies

c
o
r
r
_
s
i
n
g
l
e
(
𝑋
,
𝑁
)
=
c
o
r
r
_
i
n
t
e
g
r
a
l
(
𝑋
,
𝑁
)
.
corr_single(X,N)=corr_integral(X,N).

Therefore

(
𝑅
Λ
s
m
o
o
t
h
(
𝑋
,
𝑁
)
:
𝐶
)
=
𝑐
𝑁
 
c
o
r
r
_
i
n
t
e
g
r
a
l
(
𝑋
,
𝑁
)
,
𝑐
𝑁
:
=
1
800
1
log
⁡
(
𝑁
)
2
.
(RΛ
smooth
	​

(X,N):C)=c
N
	​

corr_integral(X,N),c
N
	​

:=
800
1
	​

log(N)
2
1
	​

.
	​


Combining with the 
𝑄
0
Q
0
	​

-split

c
o
r
r
_
i
n
t
e
g
r
a
l
(
𝑋
,
𝑁
)
=
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
a
j
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
+
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
,
corr_integral(X,N)=corr_integral_major_Q0(X,N,1)+corr_integral_minor_Q0(X,N,1),

which is exactly corr_integral_eq_major_Q0_add_minor_Q0 in CorrIntegralQ0Reduction.lean:46-63, gives

(
𝑅
Λ
s
m
o
o
t
h
(
𝑋
,
𝑁
)
:
𝐶
)
=
𝑐
𝑁
 
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
a
j
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
+
𝑐
𝑁
 
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
.
(RΛ
smooth
	​

(X,N):C)=c
N
	​

corr_integral_major_Q0(X,N,1)+c
N
	​

corr_integral_minor_Q0(X,N,1).
	​


So the exact normalized minor remainder is

𝑅
Λ
s
m
o
o
t
h
,
m
i
n
o
r
,
𝑄
0
(
𝑋
,
𝑁
)
:
=
𝑐
𝑁
 
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
,
𝑐
𝑁
=
1
800
1
log
⁡
(
𝑁
)
2
.
RΛ
smooth,minor,Q
0
	​

	​

(X,N):=c
N
	​

corr_integral_minor_Q0(X,N,1),c
N
	​

=
800
1
	​

log(N)
2
1
	​

.
	​


This is the precise bridge you can prove from the current project.

What this does and does not identify

What it does identify exactly:

ambient Hilbert variable: 
𝑁
∈
E
v
e
n
I
n
(
𝑋
,
𝐻
)
N∈EvenIn(X,H),

projector variable: 
𝛼
α,

Fourier sign: 
𝑒
(
−
𝑁
𝛼
)
e(−Nα),

normalization constant: 
𝑐
𝑁
=
(
1
/
800
)
log
⁡
(
𝑁
)
−
2
c
N
	​

=(1/800)log(N)
−2
,

object type: extractor-applied 
𝑄
0
Q
0
	​

-minor packet.

What it does not identify exactly:

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
⋅
,
1
)
≠
𝑐
𝑋
 
𝑃
𝐴
𝑐
𝑅
2
corr_integral_minor_Q0(X,⋅,1)

=c
X
	​

P
A
c
	​

R
2
	​


in the paper’s bare Route-B notation, because the Lean object already includes the outer 
𝛽
β-extractor layer, while the paper distinguishes bare 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

 from the extractor statistic 
𝑆
𝑛
0
S
n
0
	​

	​

 / 
𝑇
(
𝑦
)
T(y). The paper’s bank projector 
𝑃
𝐴
P
A
	​

 is a Fourier projection in the same frequency variable, but the variance theorem itself is stated for the extractor statistic 
𝑆
𝑛
0
S
n
0
	​

	​

, not for bare 
𝑃
𝐴
𝑐
𝑅
2
P
A
c
	​

R
2
	​

.

So the exact statement you can prove is:

c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
=
𝐶
𝑁
 ⁣
(
𝑃
𝐴
𝑋
𝑐
𝐺
𝑋
,
𝑁
)
,
corr_integral_minor_Q0(X,N,1)=C
N
	​

(P
A
X
c
	​

	​

G
X,N
	​

),
	​


and, after normalization,

𝑅
Λ
s
m
o
o
t
h
,
m
i
n
o
r
,
𝑄
0
(
𝑋
,
𝑁
)
=
1
800
log
⁡
(
𝑁
)
2
 
𝐶
𝑁
 ⁣
(
𝑃
𝐴
𝑋
𝑐
𝐺
𝑋
,
𝑁
)
.
RΛ
smooth,minor,Q
0
	​

	​

(X,N)=
800log(N)
2
1
	​

C
N
	​

(P
A
X
c
	​

	​

G
X,N
	​

).
	​


That is the bridge lemma.

Why this is enough to move forward

This tells you exactly what rawMinorPacketEnergy_canon must bound:

∑
𝑁
∈
E
v
e
n
I
n
(
𝑋
,
𝐻
)
∣
𝐶
𝑁
 ⁣
(
𝑃
𝐴
𝑋
𝑐
𝐺
𝑋
,
𝑁
)
∣
2
N∈EvenIn(X,H)
∑
	​

	​

C
N
	​

(P
A
X
c
	​

	​

G
X,N
	​

)
	​

2

up to the harmless factor 
𝑐
𝑁
2
c
N
2
	​

.

So the next proof target is no longer mysterious. It is:

treat 
𝛼
↦
𝐺
𝑋
,
𝑁
(
𝛼
)
α↦G
X,N
	​

(α) as the extracted frequency field,

project it sharply to 
𝐴
𝑋
𝑐
A
X
c
	​

,

take the 
𝑁
N-th coefficient,

sum squares in 
𝑁
N,

run TT
∗
∗
 on that unsplit packet.

Lean-friendly theorem shape

The theorem worth adding is:

theorem corr_integral_minor_Q0_eq_coeff_projected_extracted
  (X N : ℕ) :
  corr_integral_minor_Q0 X N 1
    =
  ∫ α in (0:ℝ)..(1:ℝ),
    ((majorArcSetQ0 X 1)ᶜ.indicator
      (fun α =>
        (fourier (T := (1 : ℝ)) (-(N : ℤ)) (α : UC) : ℂ) *
        (∫ β in (0:ℝ)..(1:ℝ),
          kernelPolyC (β : UC) *
            expSum X N ((α : UC) - (β : UC)) *
            expSum X N ((α : UC) + (β : UC)))) α)

proved by unfolding corr_integral_minor_Q0, unfolding innerIntegrand, and applying Fubini.

Then add:

theorem RLambda_smooth_eq_norm_mul_major_plus_minor
  (X N : ℕ) :
  ((RΛ_smooth X N : ℝ) : ℂ)
    =
  (((1/800 : ℝ) * (1 / (Real.log (N : ℝ))^2)) : ℂ) *
    (corr_integral_major_Q0 X N 1 + corr_integral_minor_Q0 X N 1)

by combining the existing two theorems in MajorArcStep11RLSmoothIntegralScaled.lean and CorrIntegralQ0Reduction.lean.

That gives you the exact bridge, with the exact scalar.

With your normalization bridge in place, the unsplit theorem should now be attacked in the normalized scale first, then converted back to corr_integral_minor_Q0.

The key reduction

Define

𝑐
𝑁
:
=
1
800
(
log
⁡
𝑁
)
2
,
𝑚
𝑋
(
𝑁
)
:
=
(
𝑅
Λ
s
m
o
o
t
h
(
𝑋
,
𝑁
)
:
𝐶
)
−
𝑐
𝑁
 
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
a
j
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
.
c
N
	​

:=
800(logN)
2
1
	​

,m
X
	​

(N):=(RΛ
smooth
	​

(X,N):C)−c
N
	​

corr_integral_major_Q0(X,N,1).

Your new bridge gives

𝑚
𝑋
(
𝑁
)
=
𝑐
𝑁
 
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
.
m
X
	​

(N)=c
N
	​

corr_integral_minor_Q0(X,N,1).

So

∣
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
∣
2
=
𝑐
𝑁
−
2
 
∣
𝑚
𝑋
(
𝑁
)
∣
2
.
	​

corr_integral_minor_Q0(X,N,1)
	​

2
=c
N
−2
	​

∣m
X
	​

(N)∣
2
.

Now for 
𝑁
∈
E
v
e
n
I
n
(
𝑋
,
𝐻
)
N∈EvenIn(X,H), we have 
𝑋
≤
𝑁
≤
𝑋
+
𝐻
X≤N≤X+H, so

log
⁡
𝑋
≤
log
⁡
𝑁
≤
log
⁡
(
𝑋
+
𝐻
)
,
logX≤logN≤log(X+H),

hence

𝑐
𝑁
≥
𝑐
min
⁡
(
𝑋
)
:
=
1
800
(
log
⁡
(
𝑋
+
𝐻
)
)
2
.
c
N
	​

≥c
min
	​

(X):=
800(log(X+H))
2
1
	​

.

Therefore

∑
𝑁
∈
E
v
e
n
I
n
(
𝑋
,
𝐻
)
∣
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
(
𝑋
,
𝑁
,
1
)
∣
2
≤
𝑐
min
⁡
(
𝑋
)
−
2
∑
𝑁
∈
E
v
e
n
I
n
(
𝑋
,
𝐻
)
∣
𝑚
𝑋
(
𝑁
)
∣
2
.
N∈EvenIn(X,H)
∑
	​

	​

corr_integral_minor_Q0(X,N,1)
	​

2
≤c
min
	​

(X)
−2
N∈EvenIn(X,H)
∑
	​

∣m
X
	​

(N)∣
2
.

So it is enough to prove the normalized energy theorem

∑
𝑁
∈
E
v
e
n
I
n
(
𝑋
,
𝐻
)
∣
𝑚
𝑋
(
𝑁
)
∣
2
≤
1
800
2
(
log
⁡
(
𝑋
+
𝐻
)
)
4
(
𝐻
𝑋
+
1
𝐻
𝑄
0
2
)
.
N∈EvenIn(X,H)
∑
	​

∣m
X
	​

(N)∣
2
≤
800
2
(log(X+H))
4
1
	​

(
X
H
	​

+
HQ
0
2
	​

1
	​

).
	​


Then rawMinorPacketEnergy_canon follows immediately.

That is the bridge you wanted.

Why this is the right theorem

The paper’s variance theorem lives in the 
𝑅
Λ
s
m
o
o
t
h
RΛ
smooth
	​

-scale, not in the raw corr_integral scale. Your bridge has now isolated exactly that normalization:

𝑅
Λ
s
m
o
o
t
h
=
𝑐
𝑁
(
major
+
minor
)
.
RΛ
smooth
	​

=c
N
	​

(major+minor).

So the object that should satisfy the paper-style variance bound is 
𝑚
𝑋
(
𝑁
)
m
X
	​

(N), not 
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
corr_integral_minor_Q0 directly.

That removes the annoying 
800
(
log
⁡
𝑁
)
2
800(logN)
2
 from the analytic proof.

A good Lean theorem to add

Define

m
i
n
o
r
R
e
s
i
d
u
a
l
N
o
r
m
(
𝑋
,
𝑁
)
:
=
(
𝑅
Λ
s
m
o
o
t
h
(
𝑋
,
𝑁
)
:
𝐶
)
−
𝑞
0
𝑁
𝑜
𝑟
𝑚
𝐹
𝑎
𝑐
𝑡
𝑜
𝑟
(
𝑁
)
 
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
a
j
o
r
_
Q
0
(
𝑋
,
𝑁
,
Δ
c
a
n
o
n
)
.
minorResidualNorm(X,N):=(RΛ
smooth
	​

(X,N):C)−q0NormFactor(N)corr_integral_major_Q0(X,N,Δ
canon
	​

).

Then prove:

theorem rawMinorPacketEnergy_canon_of_normalized_energy
  (hNorm :
    ∀ {X : ℕ}, X0 ≤ X →
      (∑ N ∈ EvenIn X H, ‖minorResidualNorm X N‖^2)
        ≤
      (((1 / 800 : ℝ)^2) / (Real.log (X + H : ℝ))^4) *
        (((H : ℝ) / (X : ℝ)) + 1 / ((H : ℝ) * (Q0 : ℝ)^2))) :
  ∀ {X : ℕ}, X0 ≤ X →
    (∑ N ∈ EvenIn X H, ‖corr_integral_minor_Q0 X N Δ_canon‖^2)
      ≤ ((H : ℝ) / (X : ℝ)) + 1 / ((H : ℝ) * (Q0 : ℝ)^2)

The proof is deterministic:

rewrite minorResidualNorm using your bridge theorem,

use

∥
c
o
r
r
_
i
n
t
e
g
r
a
l
_
m
i
n
o
r
_
Q
0
∥
2
=
𝑐
𝑁
−
2
∥
𝑚
𝑋
(
𝑁
)
∥
2
,
∥corr_integral_minor_Q0∥
2
=c
N
−2
	​

∥m
X
	​

(N)∥
2
,

use the window lower bound

𝑐
𝑁
≥
1
800
(
log
⁡
(
𝑋
+
𝐻
)
)
2
,
c
N
	​

≥
800(log(X+H))
2
1
	​

,

sum.

The proof you actually need to discover

After this reduction, the only analytic theorem left is the normalized one:

∑
𝑁
∈
E
v
e
n
I
n
(
𝑋
,
𝐻
)
∣
𝑚
𝑋
(
𝑁
)
∣
2
≤
1
800
2
(
log
⁡
(
𝑋
+
𝐻
)
)
4
(
𝐻
𝑋
+
1
𝐻
𝑄
0
2
)
.
N∈EvenIn(X,H)
∑
	​

∣m
X
	​

(N)∣
2
≤
800
2
(log(X+H))
4
1
	​

(
X
H
	​

+
HQ
0
2
	​

1
	​

).

Its proof skeleton is:

1. Rewrite 
𝑚
𝑋
(
𝑁
)
m
X
	​

(N) as a sharp minor-frequency packet

Using your bridge plus the corr_integral_minor_Q0 formula,

𝑚
𝑋
(
𝑁
)
=
𝑐
𝑁
∫
0
1
1
𝐴
𝑋
𝑐
(
𝛼
)
 
𝑒
(
−
𝑁
𝛼
)
 
(
∫
0
1
𝐾
(
𝛽
)
 
𝑆
𝑋
(
𝛼
−
𝛽
)
𝑆
𝑋
(
𝛼
+
𝛽
)
 
𝑑
𝛽
)
 
𝑑
𝛼
,
m
X
	​

(N)=c
N
	​

∫
0
1
	​

1
A
X
c
	​

	​

(α)e(−Nα)(∫
0
1
	​

K(β)S
X
	​

(α−β)S
X
	​

(α+β)dβ)dα,

where 
𝐴
𝑋
=
m
a
j
o
r
A
r
c
S
e
t
Q
0
(
𝑋
,
1
)
A
X
	​

=majorArcSetQ0(X,1).

So 
𝑚
𝑋
(
𝑁
)
m
X
	​

(N) is the 
𝑁
N-th Fourier coefficient of the extracted field after sharp projection to the 
𝑄
0
Q
0
	​

-minor set.

2. Take the 
ℓ
2
ℓ
2
 norm in 
𝑁
N first

Expand

∑
𝑁
∈
E
v
e
n
I
n
(
𝑋
,
𝐻
)
∣
𝑚
𝑋
(
𝑁
)
∣
2
N∈EvenIn(X,H)
∑
	​

∣m
X
	​

(N)∣
2

before taking absolute values inside.

This is the TT
∗
∗
 step. It produces a Toeplitz kernel in the Fourier-mode difference 
𝑡
t.

3. Split constant mode and nonzero modes

Write the resulting energy as

𝐸
0
(
𝑋
)
+
𝐸
≠
0
(
𝑋
)
.
E
0
	​

(X)+E

=0
	​

(X).

The 
𝑡
=
0
t=0 mode is the leakage / alias / Type-I piece.

The 
𝑡
≠
0
t

=0 modes are the oscillatory SSU/BG piece.

4. Use the hard minor-weight Fourier coefficients

For nonzero 
𝑡
t,

1
𝐴
𝑋
𝑐
^
(
𝑡
)
=
−
1
𝐴
𝑋
^
(
𝑡
)
.
1
A
X
c
	​

	​

	​

(t)=−
1
A
X
	​

	​

	​

(t).

So the nonzero minor kernel is the same as the hard major kernel up to sign.

That means the existing Step-5 TT
∗
∗
 major-tail algebra is exactly the right template to port.

5. Bound the two pieces

You want

𝐸
≠
0
(
𝑋
)
≤
1
800
2
(
log
⁡
(
𝑋
+
𝐻
)
)
4
𝐻
𝑋
,
E

=0
	​

(X)≤
800
2
(log(X+H))
4
1
	​

X
H
	​

,

coming from SSU/BG/dispersive cancellation,

and

𝐸
0
(
𝑋
)
≤
1
800
2
(
log
⁡
(
𝑋
+
𝐻
)
)
4
1
𝐻
𝑄
0
2
,
E
0
	​

(X)≤
800
2
(log(X+H))
4
1
	​

HQ
0
2
	​

1
	​

,

coming from Type-I/AO/alias suppression.

Then add them.

A useful discovery point

Do not insist on exact constants 
1
,
1
1,1 at this stage.

With

C
r
o
w
(
𝑎
4
,
2
)
=
4
(
𝜋
2
3
−
1
)
≈
9.15947
,
Crow(a
4,2
	​

)=4(
3
π
2
	​

−1)≈9.15947,

the diagonal raw constant can be much larger and still pass calibration. Even with square-function constant 
𝐾
=
3
K=3, you can allow

𝐶
2
≲
5.82
C
2
	​

≲5.82

and still stay under the certified cap 
160
160. So a small normalization loss, or even a modest analytic constant, is harmless.

That means the theorem you should try to prove first is not the razor-sharp

1
,
1
1,1

version, but any explicit

𝐶
2
𝐻
𝑋
+
𝐶
3
1
𝐻
𝑄
0
2
C
2
	​

X
H
	​

+C
3
	​

HQ
0
2
	​

1
	​


with 
𝐶
2
C
2
	​

 comfortably below about 
5.8
5.8 if you keep 
𝐾
=
3
K=3, and much more freedom on 
𝐶
3
C
3
	​

.

The cleanest next file

I would add a file like

Goldbach/Cert/MajorArcModules/Q0MinorNormalizedEnergy.lean

with three lemmas:

minorResidualNorm_eq_norm_mul_minor_Q0

q0NormFactor_lower_on_window

rawMinorPacketEnergy_canon_of_normalized_energy

Then the real analytic work can focus on the normalized theorem alone.